# CONTEXT.md — 弹幕情报库（danmu-intel）

> 本文件是项目的**领域词典与决策索引**。只记稳定事实：术语定义、已拍板决策、文档地图。
> 变更记录放 `docs/adr/`，需求正文放 GitHub issue。

---

## 1. 一句话定位

把电竞直播弹幕变成**可溯源、可支持决策**的情报，并且越攒越值钱。

## 2. 与蓝本的关系

- **蓝本（只读参考）**：`~/Workspace/danmu-intel-local`（GitHub 私有，468 文件）
- **本项目**：`~/Workspace/danmu-intel`（GitHub `twinstony/danmu-intel`）
- **设计权威**：`danmu-intel-local/docs/task/DANMU_INTEL_CLOUD_PYTHON_PROJECT_PRD.md`（PRD v2.0 定稿·待开发）

**两大根本差异**（本项目的立项理由）：

| 差异 | 蓝本 | 本项目 |
|---|---|---|
| LLM 提炼 | 手动（`USE_LLM=False`） | 自动（程序直连 DeepSeek） |
| 付费闭环 | 表单推 TG + 手工 `members.json` | 链上收款自动开通 |

---

## 3. 核心术语

| 术语 | 定义 |
|---|---|
| **情报节点** | 比赛生命周期中的情报产出时点：PRE（赛前）/ BP（BP 后）/ MID（局中）/ END（局末）/ OT（加时）/ FULL（整场复盘） |
| **十段结构** | 情报页唯一权威结构：`0 比赛信息 → 1 结果总览 → 2 逐局复盘 → 3 队伍画像 → 4 人员画像 → 5 灰信号汇总 → 6 联赛规律与版本 → 7 预测验证 → 8 盘口讨论 → 9 情报含义与后续观察点 → 10 数据与溯源`。A 型整场 = 0-10；B 型局中 = 0-11；赛前 = 0-8 |
| **切片** | 按节点时间窗从原始弹幕流中截取的数据段。命名 `<match_slug>_g{game>_<phase>` |
| **规则统计底料** | 纯确定性输出（词表统计/密度时间线/灰信号计数），是喂给 LLM 的结构化输入 |
| **灰信号** | 观众质疑类信号。**永远只作风险标注**，对外写「观众质疑 · 非结论」 |
| **快照冻结** | 情报页「数据截至」语义：快报生成后数据即冻结，完整版原地升级，不追加 |
| **LLM 只当打字员** | 架构原则：结构/标准/校验全在程序里，LLM 仅通过固定提示词做判断性提炼，换模型不影响质量 |
| **终局四信号** | 判定「比赛已结束」必须齐备：官方比分 + 官方系列状态 + 弹幕多信号共振 + 流量骤降 |
| **四维知识库** | 长期积累的知识资产四个维度：选手 / 队伍 / 英雄·地图 / 联赛 |
| **情报资产** | 每场比赛结束后提炼的沉淀单元：`{主题, 维度, 洞察一句话, 证据, 置信, 验证状态, 时间}` |
| **验证回填** | 灰信号兑现率 / BP 锚点应验率 / 预测命中率的持续累加 |
| **唯一充值地址** | 链上收款方案：每个用户每条链一个专属地址（行业标准，BTCPay Server 同款做法） |
| **xpub** | 扩展公钥。EVM 用它派生无限子地址而**不需要私钥**，实现 watch-only |
| **reference** | Solana Pay 规范字段（32 字节公钥）。Solana 无法 xpub 派生，改用「单一收款地址 + 唯一 reference」识别付款人 |
| **确认数** | 入账判定门槛：Polygon 3 块；Solana finalized |
| **待补款** | 入账金额不足时挂起的状态，不自动开通 |
| **宽限期** | 到期后仍可访问的天数（3 天），超期自动降级免费账号 |
| **早鸟** | 优惠档位：$39/月、$105/季、$390/年。原定限 20 名或 2026-09-15 截止 |
| **梯度涨价** | 正式订阅每满 20 名整体 +20%，月付封顶 $99、年付封顶 $986；**老用户终身锁价** |
| **Pro 墙** | 付费墙，只挂两处：进行中的节点情报 + 完整画像。比赛结束 → 该场自动转免费 |

---

## 4. 已拍板决策（用户确认）

| 决策 | 内容 | 记录 |
|---|---|---|
| 设计基准 | 直接采用 PRD v2.0，不从零设计 | ADR-0001 |
| 生成路径 | 移除多路径并存，收敛为「程序固化 + LLM 只走接口」唯一权威 | ADR-0004 |
| 收款链 | **仅 Polygon + Solana**（明确排除 Base） | ADR-0003 |
| 收款机制 | **唯一充值地址 + watch-only（不持有私钥）** | ADR-0002 |
| Solana 方案 | 单一收款地址 + 唯一 `reference` 字段（ed25519 硬约束所致） | ADR-0003 |
| 定价 | 保留现有档位与梯度规则；早鸟窗口需重设（原 2026-09-15 已过期） | 待定 |
| 收款钱包 | 新建专用收款钱包 | 待定（助记词保管方式） |
| 标签契约 | 5 个规范标签原样使用：`needs-triage` `needs-info` `ready-for-agent` `ready-for-human` `wontfix` | `docs/agents/triage-labels.md` |
| issue 粒度 | 同一需求尽量写在同一个 issue 里 | — |

---

## 5. 技术硬约束（查证后确认，非设计选择）

1. **EVM 无法附言**：ERC-20 `transfer(address,uint256)` 的 calldata 被占满，物理上塞不下备注 → 唯一地址是行业标准做法。附言/Tag 只存在于 XRP / Stellar / Cosmos / TON。
2. **Solana 无法 xpub 派生**：ed25519（SLIP-0010）只支持 hardened 派生，**不可能从公钥推子公钥**；EVM 的 secp256k1 支持 non-hardened，所以可以。→ Solana 只能用 `reference`。
3. **加密无原生 pull**：链上交易必须由私钥持有者签名，无法像 Stripe 到期自动扣款（Pull Payment Problem）→ 务实做法 = 提醒 + 手动续费。

---

## 6. 文档地图

| 文档 | 位置 | 说明 |
|---|---|---|
| PRD v2.0 | 蓝本 `docs/task/DANMU_INTEL_CLOUD_PYTHON_PROJECT_PRD.md` | **设计基准**，17 节 756 行 |
| 完整设计 v1.0 | 本仓库 `docs/design/DANMU_INTEL_DESIGN.md` | 实现级设计，11 功能层逐层实现 + 验收 |
| 设计评审 | GitHub issue #1 | 设计文档全文 + 待拍板项 |
| 服务器 PRD | 蓝本 `docs/task/DANMU_INTEL_SERVER_PRD_2026-08-31.md` | 部署细节 |
| 产品框架 | 蓝本 `docs/task/INTEL_PRODUCT_FRAMEWORK_2026-08-31.md` | 引擎 + 模板口径 |
| 订阅台账 | 蓝本 `docs/task/SUBSCRIPTION_LEDGER.md` | 现有订阅记录（手工维护，待迁移） |
| 定价 | 蓝本 `docs/task/PRICING_ESPORTS_BUNDLE_2026.md` | 档位与梯度规则 |
| 模板权威 | 蓝本 `knowledge/INTEL_TEMPLATE_OLD_2026-08-31.md` | 情报页唯一模板标准 |

---

## 7. 环境

- 代理：`http://127.0.0.1:7897`（clone/push/gh 走代理；**不写全局 git 配置**，用 `-c http.proxy=` 一次性传入）
- 蓝本路径：`~/Workspace/danmu-intel-local`（只读）
