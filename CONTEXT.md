# 领域术语表（CONTEXT.md）

> 项目所有文档、issue、代码标识符使用本表定义的术语。**不使用同义词漂移**。

## 核心术语

| 术语 | 定义 | 英文 / 代码标识符 | 避免用 |
|---|---|---|---|
| 比赛 | 一场完整的电竞赛事（含若干小局） | `match` | 对局、场次 |
| 小局 | 一场比赛中的单局（一局一局独立计分） | `game` / `game_no` | 回合、局 |
| 切片 | 按小局边界切分后的时间段（含起止与来源） | `slice` | 切段、片段 |
| 边界来源 | 切片边界凭什么定：官方 / 弹幕信号 / 报告窗口 / 人工修正 | `boundary_source` | 边界类型 |
| 边界候选 | 某个来源给出的小局边界提议（未必被采用） | `BoundaryCandidate` | 备选边界 |
| 冲突记录 | 多来源给出**不同**边界时留下的事实（高优先级胜出照样记录） | `conflict_note` | 冲突说明 |
| 人工修正 | 操作者覆盖已有切片边界，必须带操作者与理由 | `override_by` / `override_at` / `override_reason` | 手工修、手动改 |
| 边界复核 | 弹幕候选边界需 ≥2 类独立信号支持才能采用 | `review` | 审核 |
| 原始弹幕 | 从直播间直接采集的未加工文本流 | `raw danmu` / `DanmuEvent` | 原始消息 |
| 规则统计 | 纯函数计算的客观指标（密度曲线/峰值/比分等） | `metrics` | 统计数据 |
| 灰信号 | 仅作风险提示、不指控、不点名的信号 | `gray_signal` | 假信号、异常信号 |
| 证据门槛 | 灰信号的多主体多时段门槛（命中 ≥N 次、独立发言者 ≥M 人、覆盖 ≥K 个时段） | N/M/K | 阈值 |
| 作废灰信号 | 未达门槛、已留原因、不进报告的灰信号 | `discarded` | 排除、过滤 |
| 独立信号 | 互不依赖的信号类别（终局判定要 ≥3 类，边界复核要 ≥2 类） | independent signal | 信号种类 |
| 终局判定 | 对「比赛已终局」的判定结果：`final` / `live` / `revoked` | `final_signal` | 结束判定 |
| 终局信号 | 满足≥3类独立信号同时成立且2分钟无反转 | `final_signal` | 结束信号 |
| 事实层 | 报告中完全来自官方/规则统计的部分 | `fact layer` | — |
| 解读层 | 报告中 LLM / 规则直出生成的解读部分 | `interpretation layer` | 分析层 |
| 赛中快报 | 关键节点结束后 ≤2 分钟内上线的快报 | `live_brief` | 快评 |
| 完整版 | 比赛结束后 ≤10 分钟内上线的完整报告 | `full` | — |
| 复盘版 | 比赛结束后 ≤15 分钟上线的赛后复盘 | `review` | — |
| 付费墙 | 按比赛状态机决定是否对内容加锁的机制 | `paywall` | — |
| 订单 | 一个收款要求（含专属地址/金额/memo/到期） | `order` | — |
| 订单状态 | 订单状态机：`pending`（待付）/`short`（待补款）/`paid`（已付）/`expired`（过期） | `orders.status` | 状态、进度 |
| 公开引用 | 订单对外展示的短引用（同时是 Solana 的 memo） | `public_ref` | 订单号 |
| 金额尾数 | 同一档位订单金额上叠加的唯一小额尾数（区分同额多单） | `amount_due_units` 的尾数 | 零头 |
| 待补款 | 入账不足应收时的订单状态，须告知差额 | `short` / `shortage_units` | 欠款 |
| 领取令牌 | 下单时下发、只存哈希、用于领取凭据的一次性令牌 | `claim_token` / `claim_hash` | 取件码 |
| 派生地址 | 从 xpub 按 BIP44 路径派生的 watch-only 地址 | `derived_address` | — |
| 派生索引 | 派生地址的序号，只前进不回退（已展示的地址不分配给别人） | `orders.address_index` | 序号 |
| 档位 | 订阅档位：`standard`（标准档）/`trial`（试用档，每人一次） | `tier` | 套餐 |
| 会员 | 凭既有通讯账号标识的订阅者 | `member` | 用户、账号 |
| 会员状态 | 会员状态机：`pending`/`active`/`grace`（宽限）/`expired`/`revoked` | `members.status` | — |
| 宽限期 | 到期后仍可访问的一段时间（默认 24 小时） | `grace_ms` | 缓冲期 |
| 领取 | 凭「通讯账号 + 订单引用 + 领取令牌」把凭据发到浏览器 | `claim` | 获取、申请 |
| 校验 | 凭凭据确认会员状态与有效期 | `verify` | 认证 |
| 凭据 | 会员领取的访问凭证（存哈希不下明文） | `credential` | token |
| 对账 | 把链上入账对到订单上（足额开通 / 不足记差额 / 对不上账提醒） | `settle` | 匹配、核销 |
| 发布批次 | 一次原子发布（产物+检查+回滚指针） | `release` | — |
| 站点树 | 一次发布的完整静态产物（页面集合，检查与替换的对象） | `SiteTree` | 站点目录、产物集 |
| 发布版本 | 发布批次的递增序号（回滚后能指出「线上是哪一版」） | `releases.version` | 构建号 |
| 发布检查 | 发布前必须通过的检查（需求 §6.8 的 6 项 + 1 项来源可达加固） | `publish/checks.py` | 上线检查 |
| 付费正文 | 付费墙内、**不进静态产物**的报告段正文（只经凭据 API 返回） | `paid_content` | 会员内容 |
| 速览卡 | 已被废弃的 UI 组件（不得出现在新产物中） | `quick_card` | — |

## 数据模型术语

| 术语 | 定义 |
|---|---|
| 适配器 | 平台特定采集逻辑（解析/流/探测），统一实现 `Adapter` 协议 |
| 用户哈希 | 平台用户 ID 的加盐哈希（`user_hash`），原始记录只存它，不落明文身份 |
| 房间会话 | 一次采集会话（进程级），含 PID、心跳、状态 |
| 采集监督 | 一房间一子进程的启动/监控/重拉机制（`supervisor`），含退避与重启上限 |
| 心跳 | 子进程每 5 秒原子写的存活证据（库内 `room_sessions` 行 + `runtime/heartbeat/<platform>-<room_id>.json`） |
| 异常事件 | 采集异常产生的待投递事件（`incident` → `notifications(state='pending')`），投递归 T11 |
| 消息指纹 | 一条原始记录的内容指纹（`msg_hash` = `ts\|user_hash\|text`），去重键 `(platform, room_id, msg_hash)` |
| 贡献量 | 一个直播间对某场比赛的落盘条数 / 时间跨度 / 去重后条数（`contribution`） |
| 段 | 报告的十一分之一，固定编号 0–10，带 `kinds` 标记（`fact` / `interpretation`） |
| 段性质 | 一段的内容性质（需求 §6.6 原文，如「事实 + 解读」），由 `kinds` 派生（`nature`） |
| 来源引用 | 报告中每项事实对应的落盘文件 + **行范围** + SHA256（`SourceRef`） |
| 报告形态 | 报告的三种交付时点之一（`live_brief` / `full` / `review`），决定段集与时限 |
| 报告版本 | 同场同形态报告的版本次号（`reports.version`），只增不改，旧版可溯 |
| 事实层哈希 | 解读层输入的指纹（`fact_layer_hash`），用于回溯报告依据的事实层 |
| 算法版本 | 规则统计的算法标识（`algo_version`），变更即递增，重算写新行不覆盖旧行；人工修正按次数递增（`1.0.0+ov1`） |
| 发布批次 | `releases` 表的一行：一次原子发布（产物 + 检查 + 回滚指针） |
| 已完成节点 | 已结束、可进报告正文的小局；进行中的节点不进赛中快报（`completed_games`） |
| 额度记账 | 每次链上数据调用记 `quota_usage`，按供应商按天 |
| 入账 | 一笔到收款地址的链上转账（两链统一形状：网络/地址/交易标识/资产/金额/memo） | `Transfer` | 到账记录、转账流水 |
| 监听目标 | 一个被监听的收款地址（网络 + 地址，游标粒度就是它） | `WatchTarget` | 监控对象、钱包 |
| 监听游标 | 「上次扫到哪」的持久化位置（一目标一行） | `chain_cursors(network, scope)` | 断点、进度 |
| 补扫 | **不看游标**、按地址查全历史的一次扫描（与增量轮询互为兜底） | `rescan` | 重扫、回扫 |
| 时效闸门 | 通知 5 分钟未送达即销毁的统一机制 |
| 配置版本号 | 后台保存配置时递增，用于跨进程失效缓存 |
| 每日盐 | 统计中用于哈希 IP+UA 的每日更换盐值（次日不可回溯） |
| 中立指标 | 只计数、不褒贬、不归因的双方提及量对比（`neutral`） |
| 密度骤变 | 滑窗计数显著高于邻近窗口的局部变化（`density_shift`） |

## 缩写

| 缩写 | 全称 |
|---|---|
| LLM | Large Language Model（本报告特指 DeepSeek） |
| xpub | BIP32 Extended Public Key（watch-only，派生地址用） |
| JSONL | JSON Lines（一行一条记录） |
| SHA256 | 哈希算法，用于证据文件校验 |
| WAL | Write-Ahead Logging（SQLite 并发模式） |
| Funnel | Tailscale Funnel（公网暴露机制） |
| DAS | Digital Asset Standard（Helius Solana API 体系） |
| beacon | 统计上报的前端轻量请求 |
