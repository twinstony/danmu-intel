"""后台（C8）：独立进程 + 仅 tailnet + 单管理员 + 12 个页面 + 配置 1 分钟生效。

模块分工：

| 模块 | 负责 |
|---|---|
| `auth.py` | 口令哈希、短期签名 cookie、tailnet 判定（纯函数，可脱离 HTTP 断言） |
| `html.py` | 极简 HTML 渲染工具（转义、布局、表格、表单）—— 与站点产物同一路子，零前端构建链 |
| `pages.py` | 12 个页面（只读视图）：概览 / 比赛 / 房间 / 切片 / 灰信号 / 报告 / 发布 / 会员与订单 / 通知 / 配置 / 审计 / 成本 |
| `actions.py` | 写操作（状态机、房间、切片、灰信号、报告、发布回滚、会员、配置）—— **全部入 `audit_log`** |
| `server.py` | aiohttp 应用：tailnet 中间件 + 鉴权中间件 + 路由（独立进程，ADR-0007） |
"""
