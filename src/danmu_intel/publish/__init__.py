"""发布层：站点树 → 7 项检查 → 原子发布 → 回滚（设计 §11，ADR-0015）。

| 模块 | 职责 |
|---|---|
| `site.py` | 从库里的比赛与报告账本生成整棵站点树（产物） |
| `checks.py` | 需求 §6.8 的 6 项检查 + 1 项来源可达加固（逐项纯函数） |
| `release.py` | 原子替换 + `releases` 发布批次账本 + 幂等 + 秒级回滚 + 结束转公开 |
| `access.py` | 付费正文的唯一出口（会员凭凭据、比赛结束后所有人可见） |
| `vercel.py` | Vercel 部署查询与即时回滚（注入缝，凭据只在仓库外 `.env`） |
"""
